import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/directory/directoryRoute/spec')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/directory/directoryRoute/spec"!</div>
}
